<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wuzzaf">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission. ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.EmploySuggetDetails"></activity>
        <activity android:name=".activities.JobSuggestionDetails" />
        <activity android:name=".activities.uploadFile" />
        <activity android:name=".activities.EmployResult" />
        <activity android:name=".activities.CompanyResult" />
        <activity android:name=".activities.CompanyRequirment" />
        <activity android:name=".activities.JopSugisition" />
        <activity android:name=".activities.EmployCvActivity" />
        <activity android:name=".activities.CompanyMain" />
        <activity android:name=".activities.CandidatMain" />
        <activity android:name=".activities.Signup" />
        <activity android:name=".activities.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity" />
    </application>

</manifest>